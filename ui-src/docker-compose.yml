services:
  uzon-mail-dev:
    container_name: uzon-mail-dev
    image: node:20.11.0
    volumes:
      - ./:/app/uzon-mail
    working_dir: /app/uzon-mail
    ports:
      - 9000:9000
    # 下面两行是当无法启动容器时，可以打开注释，进入容器进行操作
    init: true
    command: [ "sh", "-c", "exec sleep infinity" ]
